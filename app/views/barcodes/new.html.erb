<h1>Producto Consultado - Codigo de Barras</h1>

<div class = "container mt-5 text-justify product-background" style="max-width: 70%;">
  <div class="text-overlay">
    <h1><strong>Nueva Consulta de Producto</strong></h1>
    <div>
      <h2>Instrucciones: </h2>
      <div>
        <p> <strong>Toma una foto al código de barras del alimnento que deseas verificar como apto para tu consumo</strong>
      </div>
      </p>
    </div>
    <div>
      <%= simple_form_for [@user, @profile, @barcode] do |f| %>
        <%= f.input :photo, as: :file, label: "Foto delantera" %>
        <%= f.submit "Escanear", class: "btn btn-primary fs-5" %>
      <% end %>
    </div>
  </div>
  <div>
    <%= link_to "Volver a Mi Perfil", user_profile_path(@user, @profile) %>
  </div>
</div>
