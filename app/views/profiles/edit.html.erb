<%= form_with(model: [@user, @profile], local: true) do |form| %>
  <h1>Edit Profile</h1>

  <%= render 'form', form: form %>

  <div class="actions">
    <%= form.submit 'Update Profile' %>
  </div>

  <%= link_to 'Back', user_profile_path(@user, @profile) %>
<% end %>
